---
layout: layouts/base.njk
title: Home
pagefind_ignore: true
---
<section class="space-y-6">
  <div class="space-y-3">
    <h1>Onchain Art Wiki</h1>
    <p class="italic">
      A reference for crypto-native and onchain art practices, mechanisms, and technical details.
    </p>
  </div>

  <div class="space-y-4">
    <h2>Articles</h2>
    <ul class="list-none space-y-3 pl-0">
      {% for article in collections.articles %}
      <li>
        <a class="relative block rounded-xl border border-zinc-200 bg-white p-5 shadow-sm transition-colors no-underline hover:bg-zinc-100"
          href="{{ article.url }}">
          <span
            class="absolute right-2 top-2 rounded bg-zinc-100 px-1.5 py-1 text-[10px] font-medium uppercase leading-none tracking-wide text-zinc-600">
            {{ article.data.chain }}
          </span>
          <span class="pr-14 text-lg font-semibold">{{ article.data.title }}</span>
          {% if article.data.summary %}
          <p class="mt-2 text-sm text-zinc-700">{{ article.data.summary }}</p>
          {% endif %}
        </a>
      </li>
      {% endfor %}
    </ul>
  </div>
</section>